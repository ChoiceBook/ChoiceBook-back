const express = require('express');
const router = express.Router();
const pool = require('../config/database');

// Get ranks by plot ID and user ID


// Define other rank routes here

module.exports = router;
